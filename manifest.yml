---
applications:
- name: redispubsub
  framework: node
  runtime: node08
  memory: 64M
  instances: 1
  url: redispubsub.${target-base}
  path: .
  services:
    redis-b2c43:
      vendor: redis
      version: '2.6'
      tier: free
